day1
今天移植了KEY OLED I2C软件读取MPU6050，定时器等工程模块进入我的平衡车并根据我的平衡车板子配置了相关模块 还对相关模块进行了测试
进行了相关模块的测试并遇到了一下问题
引脚映射错误：按键4硬件接PA4，代码却读取PB4，导致无反应。修正端口后正常。教训：移植代码必须逐项核对GPIO引脚与硬件一致。
扫描频率错误：定时中断周期1秒，按键扫描间隔长达20秒，无法捕捉短按。将定时器改为100Hz，消抖阈值降为2（20ms）后正常。教训：非阻塞按键扫描周期需设置在10~20ms，否则会丢失用户操作。
测试在读取MPU6050时发现了I2C接受应答那里有一个if(MyI2C_R_SDA()==1)这里==1存在时我的中断判断那里中断重叠了并且cnt显示32 在==1被删除后中断正常，cnt变成 969原因查了没搞明白，明天再用相关分析仪测一下具体原因

ay2
今天完成了剩下模块的移植其中遇到了一些问题，并且找寻了昨天相关问题的原因但由于没有示波器等器件问题并没有找出来但是今天遇到了一些问题也都解决了
问题1：
现象：右边电机仅在 PWMR < 0 才转，PWMR ≥ 0 不转。
原因：正转需 PB14 高电平，但因硬件故障（短路/虚焊）PB14 被强制拉低，方向信号错误致电机不转；反转 PB14 本就为低，故正常。
解决：更换 PB14 引脚或修复硬件。
问题2：
电机不转， 方向引脚开漏无上拉，改为推挽输出恢复正常。
电机转速慢，发现电池并不供电，换室友电池后恢复供电，电机转速恢复正常。
左右控制交叉，PWM/方向引脚接线交叉，按代码逻辑重接。
编码器速度恒零未调 Encoder_Init()，TIM4 时基误写为 ICInit，但更改后还未恢复，后面看了pcb板后，发现pcb板 VCC 与 GND 和编码电机接反，更改后，正确供电恢复正常。
教训：供电与接线是硬件第一关；初始化要完整；正负极千万别反。

